{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "cd menu-eureka-api && chmod +x wait-for-it.sh && ./wait-for-it.sh $PGHOST 'python manage.py migrate && python manage.py createsuperuser --noinput && gunicorn core.wsgi:application --bind 0.0.0.0:$PORT --workers 4 --threads 4 --timeout 180'",
    "healthcheckPath": "/",
    "healthcheckTimeout": 900,
    "restartPolicyType": "on_failure",
    "restartPolicyMaxRetries": 15,
    "healthcheckInterval": 60
  }
} 